# 📌 راهنمای مدیریت نسخه و دیپلوی خودکار (Versioning & Deployment Guide)

این پروژه از معماری **«منبع واحد حقیقت» (Single Source of Truth)** برای مدیریت نسخه‌ها استفاده می‌کند. تمام اجزای سیستم (کد برنامه، رابط کاربری سایت، تگ‌های گیت و استقرار کلودفلر) کاملاً به یکدیگر متصل و یکپارچه شده‌اند.

---

## 🏗️ معماری یکپارچگی نسخه‌ها

```
[ package.json ]  ──(بیلد Vite)──>  نمایش خودکار نسخه در هدر سایت (Header.tsx)
       │
 (npm version)
       │
       ▼
 [ Git Tag (v*) ] ──(git push)──>  GitHub Actions  ──>  دیپلوی روی Cloudflare Workers
```

1. **`package.json`**: مرجع اصلی شماره نسخه است.
2. **`Header.tsx`**: نسخه را مستقیماً از فایل پیکربندی Vite (`__APP_VERSION__`) می‌خواند و هیچ متنی به صورت دستی یا هاردکد نیست.
3. **گیت و کلودفلر**: با زدن تگ گیت، فرآیند تست، بیلد و استقرار نسخه جدید در کمتر از ۱ دقیقه به صورت خودکار انجام می‌شود.

---

## 🚀 نحوه انتشار نسخه جدید (تنها در ۲ مرحله)

برای انتشار نسخه جدید، فقط کافیست دو دستور زیر را در ترمینال اجرا کنید:

### گام اول: بالا بردن شماره نسخه (Version Bump)
یکی از ۳ دستور زیر را متناسب با حجم تغییرات خود انتخاب کنید:

```bash
# برای رفع باگ و تغییرات کوچک (مثلاً 2.4.0 -> 2.4.1)
npm version patch

# برای اضافه کردن ویژگی‌های جدید (مثلاً 2.4.0 -> 2.5.0)
npm version minor

# برای بازطراحی بزرگ یا تغییرات اساسی (مثلاً 2.4.0 -> 3.0.0)
npm version major
```

> **دستور `npm version` چه کارهایی به صورت خودکار انجام می‌دهد؟**
> 1. شماره نسخه را در `package.json` و `package-lock.json` تغییر می‌دهد.
> 2. یک کامیت گیت با نام نسخه جدید ثبت می‌کند.
> 3. یک تگ گیت با پیشوند `v` (مانند `v2.4.1`) می‌سازد.

---

### گام دوم: ارسال به گیت‌هاب و شروع استقرار خودکار
پس از دستور بالا، کافیست کامیت و تگ جدید را پوش کنید:

```bash
git push origin main --tags
```

**کار تمام است!** گیت‌هاب اکشن بلافاصله فعال می‌شود، پروژه را با نسخه جدید بیلد می‌کند و نسخه نهایی را روی کلودفلر مستقر می‌سازد.

---

## ⚙️ پیش‌نیازهای یک‌بار مصرف (فقط برای اولین راه‌اندازی)

اگر هنوز توکن‌های دسترسی را به گیت‌هاب نداده‌اید، مراحل زیر را یک‌بار انجام دهید:

1. **افزودن سکرت‌ها به گیت‌هاب**:
   - به صفحه مخزن در گیت‌هاب بروید: `Settings` ➔ `Secrets and variables` ➔ `Actions` ➔ `New repository secret`.
   - سکرت **`CLOUDFLARE_API_TOKEN`**: در کلودفلر از بخش پروفایل `API Tokens` ➔ با قالب `Edit Cloudflare Workers` یک توکن بسازید.
   - سکرت **`CLOUDFLARE_ACCOUNT_ID`**: از داشبورد کلودفلر (بخش Workers & Pages در ستون راست) کپی کنید.

2. **قطع بیلد خودکار داشبورد کلودفلر**:
   - در داشبورد کلودفلر وارد پروژه شوید: `Settings` ➔ `Builds & deployments`.
   - بخش Automatic deployments را روی **Pause** بگذارید تا با هر کامیت ساده به برنچ main بیلد انجام نشود و دیپلوی‌ها منحصراً تحت کنترل تگ‌های رسمی نسخه باشند.

---

## 🔄 شرایط اضطراری: بازگشت سریع به نسخه قبلی (Instant Rollback)

اگر نسخه‌ای منتشر کردید که باگی داشت، نیازی به کامیت یا بیلد اضطراری ندارید:
1. وارد پنل کلودفلر شوید و روی Worker پروژه کلیک کنید.
2. به بخش **Deployments** بروید.
3. در لیست نسخه‌های گذشته، روی نسخه سالم قبلی کلیک کرده و گزینه **Rollback** را انتخاب کنید.
4. ظرف چند ثانیه کل ترافیک بدون داون‌تایم به نسخه پایدار قبل برمی‌گردد!

---

## 🛠️ دیپلوی دستی (بدون تگ جدید)
در صورتی که بخواهید بدون تغییر شماره نسخه، آخرین کد را مجدداً دیپلوی کنید:
- در مخزن گیت‌هاب، به تب **Actions** بروید.
- گردش کار **Deploy to Cloudflare Workers** را انتخاب کنید.
- روی دکمه **Run workflow** کلیک کنید.
